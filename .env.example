# ICS to Google Calendar Sync Configuration
# Copy this file to .env and update with your values

# Required: ICS feed URL to sync from
ICS_URL=https://outlook.office365.com/owa/calendar/YOUR_CALENDAR_URL/calendar.ics

# Required: Google Calendar ID (use "primary" for your main calendar)
CALENDAR_ID=primary

# Optional: Sync flags (default: --dry-run for safety)
# Examples:
#   --dry-run                    # Preview changes without applying
#   --future-only               # Only sync future events
#   --prune-missing             # Delete old events not in feed
#   --future-only --prune-missing  # Combine multiple flags
SYNC_FLAGS=--dry-run

# Optional: Custom paths (use defaults if not specified)
# CREDENTIALS_PATH=/app/credentials/credentials.json
# TOKEN_PATH=/app/credentials/token.json
